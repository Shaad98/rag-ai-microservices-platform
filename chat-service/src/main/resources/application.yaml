spring:
  application:
    name: ${SERVICE_NAME}

  # datasource:
  #   url: ${DB_URL}
  #   username: postgres
  #   password: root
  #   driver-class-name: org.postgresql.Driver
  
  # jpa:
  #   hibernate:
  #     ddl-auto: update
  #   database-platform: ${HIBERNATE_DIALECT}

  # data:
  #   redis:
  #     host: ${REDIS_HOST}
  #     port: ${REDIS_PORT:6379}
      # password: ${REDIS_PASSWORD:}
    
  config:
    import: configserver:${IDENTITY_CONFIG_URL}